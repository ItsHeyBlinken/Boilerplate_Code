services:
  showcase:
    build:
      context: .
      dockerfile: Dockerfile
    image: boilerplate-showcase:latest
    container_name: boilerplate-showcase
    # Platform/internal port is 3000 (nginx listens on 3000 in-container)
    ports:
      - "${SHOWCASE_PORT:-3000}:3000"
    restart: unless-stopped
